{
  "name": "drift",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A shared world of slow objects.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "seed": "node scripts/seed-world.js",
    "seed:prod": "node scripts/seed-world.js"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
